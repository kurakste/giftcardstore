export const styles = theme => ({
    root: {
        display: 'flex',
        flexWrap: 'wrap',
    },
    formControl: {
        marginLeft: 0,
        width: '100%',
        margin: theme.spacing.unit,
        minWidth: 120,
        maxWidth: 500,
    },
    noLabel: {
        marginTop: theme.spacing.unit * 3,
    },
});
